import React from 'react';
import SongList from './SongList';

// create functional component
const App = () => {
    return (
        <div>
            <SongList />
        </div>
    );
};

export default App;